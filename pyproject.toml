[project]
name = "upsp-base"
version = "0.1.2"
description = "UPSP Base implementation — deterministic AI persona disaster-recovery substrate"
requires-python = ">=3.10"

[project.optional-dependencies]
test = [
    "pytest>=9,<10",
    "pytest-xdist>=3.8,<4",
    "pytest-timeout>=2.4,<3",
]
